# Getting Started

Welcome to the Order Food App documentation!

## Installation

1. Clone the repository:
   ```sh
   git clone <your-repo-url>
   ```
2. Install dependencies:
   ```sh
   yarn install
   ```
3. Set up your environment variables:
   - Copy `.env.example` to `.env` in your project root.
   - Fill in the required variables, or leave them empty for now if you don't have values yet.
   - Example:
     ```env
     DATABASE_URL=
     NEXTAUTH_SECRET=
     CLOUDINARY_URL=
     NODE_ENV=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
NEXTAUTH_URL=
NEXT_PUBLIC_BASE_URL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
     # ...add other variables as needed
     ```
4. Run the development server:
   ```sh
   yarn dev
   ```

## Features
- User authentication
- Menu browsing
- Cart and checkout
- Admin panel

## API Reference
See the API section for details on available endpoints.

## Deployment
Follow the deployment guide in the main README or this documentation.
